[project]
name = "mcp-hunter"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pre-commit>=4.6.0",
    "requests>=2.34.2",
]
